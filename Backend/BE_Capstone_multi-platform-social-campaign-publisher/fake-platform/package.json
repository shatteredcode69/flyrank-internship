{
  "name": "fake-social-platform-server",
  "version": "1.0.0",
  "description": "Standalone fake social platform server: simulates OAuth, rate limits, idempotency, and signed delivery webhooks for the capstone. Written from scratch per README Engineering Decisions (no starter was provided to inspect).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
